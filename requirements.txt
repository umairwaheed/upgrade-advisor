bedrock-agentcore>=1.19.0
strands-agents>=1.50.0
boto3>=1.40.0

# Browser tool driver (AgentCore Browser speaks CDP; Playwright is the client).
# Optional: the changelog tool degrades to an HTTP fallback if this is absent.
playwright>=1.49.0
